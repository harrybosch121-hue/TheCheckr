# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg

# Runtime data files (generated on server, not needed in repo)
approved.txt
approve.txt
user_stats.json
# NOTE: pending_batches.json is NOT ignored — it must be in the repo
# so Railway can resume batches after redeploy. The bot updates it at runtime.

# User uploads (generated at runtime)
uploads/

# User proxies (runtime data)
ng/user_proxies.txt
ng/user_prox.txt

# Proxy files (runtime)
working_proxies.txt
px.txt

# Backup files
*.bak
*.tmp

# Environment
.env
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
